import "reflect-metadata";

describe("Main", () => {
  beforeEach(() => {});

  it("should publish Content successfully", async () => {
    // arrange

    // act

    // assert
    expect(true).toBe(true);
  });
});
